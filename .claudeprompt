Follow all standards in CLAUDE.md and .cursor/rules/*.mdc. NEVER commit secrets - use .env files (gitignored) and .env.example (committed). ALL bash scripts must have '#!/usr/bin/env bash' and 'set -euo pipefail'. Quote bash variables. Python functions need type hints. YAML uses .yaml extension and 2 spaces. Run ./scripts/run-precommit.sh before commits. Check .gitignore before creating files. Document changes in README.md. Fail loudly with clear error messages. Be idempotent and safe by default.
